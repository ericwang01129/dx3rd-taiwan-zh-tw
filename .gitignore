# LevelDB runtime artifacts (not needed by Foundry, regenerated on launch)
packs/**/LOCK
packs/**/LOG
packs/**/LOG.old

# Standalone web page project (not part of the Foundry module distribution)
web/
